<%= form_with model: @account do |form| %>
  <div>
    <%= form.label :name %>
    <%= form.text_field :name %>
    <%= field_error(@account, :name) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>